// We need to import the source of the ES Modules
// This version of handlebars uses dynamic modules in the default index.js file

import Handlebars from 'handlebars/lib/handlebars';
export const compile = Handlebars.compile;